name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            frontend/package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Start Postgres and backend
        run: docker compose up --build -d

      - name: Wait for backend to be ready
        run: timeout 60 bash -c 'until curl -sf http://localhost:8080/event-types > /dev/null; do sleep 2; done'

      - name: Start frontend dev server
        run: npm run dev &
        working-directory: frontend

      - name: Wait for frontend to be ready
        run: timeout 30 bash -c 'until curl -sf http://localhost:5173 > /dev/null; do sleep 2; done'

      - name: Run Playwright E2E tests
        run: npx playwright test
        env:
          CI: "true"

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: test-results
          path: test-results/
          retention-days: 7
